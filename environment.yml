name: mast-book
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - xarray-datatree
      - pandas
      - intake
      - matplotlib
      - requests
      - zarr
      - xarray
      - pyspark
      - dask
      - jupyter-book
      - ghp-import
      - pyyaml
      - s3fs
      - scikit-image
      - gql
  
